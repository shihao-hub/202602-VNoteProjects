# 阶段 0: 环境准备与项目概览

## 📚 学习目标

- 理解项目的整体架构和目的
- 搭建完整的开发环境
- 能够运行项目并观察其工作流程

---

## 🎯 项目核心理解

### 项目概述

这是一个**全栈 AI 研究助手**应用,展示了如何使用 React 前端和 LangGraph 后端构建智能研究系统。该项目能够:

1. 动态生成搜索查询
2. 使用 Google Search API 进行网络研究
3. 通过反思机制识别知识缺口
4. 迭代优化搜索策略
5. 生成带引用的综合性答案

### 技术栈

#### 前端技术
- **React** - 用户界面框架
- **Vite** - 构建工具
- **Tailwind CSS** - 样式框架
- **Shadcn UI** - UI 组件库
- **TypeScript** - 类型安全的 JavaScript

#### 后端技术
- **LangGraph** - AI Agent 编排框架
- **FastAPI** - Python Web 框架
- **Google Gemini** - 大语言模型系列
  - Gemini 2.0 Flash - 快速查询生成
  - Gemini 2.5 Pro - 深度推理和答案合成
- **LangChain** - LLM 应用开发框架

#### 基础设施
- **Redis** - Pub-sub 消息代理(生产环境)
- **PostgreSQL** - 状态持久化和任务队列(生产环境)
- **Docker** - 容器化部署

---

## 📁 项目目录结构

```
study-gemini-fullstack-langgraph-quickstart/
├── frontend/                 # React 前端应用
│   ├── src/
│   │   ├── App.tsx          # 主应用组件
│   │   ├── components/      # UI 组件
│   │   │   ├── ActivityTimeline.tsx    # 活动时间线
│   │   │   ├── ChatMessagesView.tsx    # 聊天消息视图
│   │   │   ├── InputForm.tsx           # 输入表单
│   │   │   └── WelcomeScreen.tsx       # 欢迎屏幕
│   │   ├── lib/             # 工具库
│   │   └── main.tsx         # 入口文件
│   ├── package.json
│   └── vite.config.ts       # Vite 配置
│
├── backend/                 # LangGraph 后端应用
│   ├── src/
│   │   └── agent/
│   │       ├── graph.py              # LangGraph 核心图结构
│   │       ├── state.py             # 状态定义
│   │       ├── prompts.py           # 系统提示词
│   │       ├── utils.py             # 工具函数
│   │       ├── configuration.py     # 配置管理
│   │       ├── tools_and_schemas.py # 工具和数据模式
│   │       └── app.py               # FastAPI 应用
│   ├── examples/
│   │   └── cli_research.py          # CLI 示例
│   ├── langgraph.json       # LangGraph 配置
│   └── pyproject.toml       # Python 项目配置
│
├── docs/                    # 文档目录
├── Dockerfile               # Docker 构建文件
├── docker-compose.yml       # Docker Compose 配置
└── README.md                # 项目说明
```

---

## 🔧 环境配置

### 前置要求

1. **Node.js 18+** - JavaScript 运行时
2. **Python 3.11+** - Python 运行时
3. **GEMINI_API_KEY** - Google Gemini API 密钥

### API 密钥获取

访问 [Google AI Studio](https://aistudio.google.com/app/apikey) 获取免费的 Gemini API 密钥。

### 环境变量配置

在 `backend/` 目录下创建 `.env` 文件:

```bash
cd backend
cp .env.example .env
```

编辑 `.env` 文件:

```env
GEMINI_API_KEY="YOUR_ACTUAL_API_KEY"
```

### 依赖安装

#### 后端依赖

```bash
cd backend
pip install .
```

**核心依赖说明**:
- `langgraph>=0.2.6` - LangGraph 核心库
- `langchain>=0.3.19` - LangChain 核心库
- `langchain-google-genai` - Google Gemini 集成
- `python-dotenv>=1.0.1` - 环境变量管理
- `langgraph-sdk>=0.1.57` - LangGraph SDK (前端使用)
- `langgraph-cli` - LangGraph 命令行工具
- `langgraph-api` - LangGraph API 服务器
- `fastapi` - Web 框架
- `google-genai` - Google GenAI SDK

#### 前端依赖

```bash
cd frontend
npm install
```

### 开发服务器启动

#### 方式 1: 使用 Makefile (推荐)

在项目根目录:

```bash
make dev
```

这将同时启动前端和后端开发服务器。

#### 方式 2: 分别启动

**后端服务器**:

```bash
cd backend
langgraph dev
```

- API 地址: `http://127.0.0.1:2024`
- LangGraph UI: 浏览器自动打开

**前端服务器**:

```bash
cd frontend
npm run dev
```

- 前端地址: `http://localhost:5173`
- 应用页面: `http://localhost:5173/app`

---

## 🏃 运行验证

### 检查清单

完成以下检查确认环境配置正确:

- [ ] 前端能正常访问 (http://localhost:5173/app)
- [ ] 后端 LangGraph UI 能打开
- [ ] 能完成一次完整的查询并看到活动时间线

### 测试查询示例

1. 打开前端应用: http://localhost:5173/app
2. 输入问题: "What are the latest trends in renewable energy?"
3. 选择研究深度 (effort): Low/Medium/High
4. 观察实时活动时间线:
   - Generating Queries (查询生成)
   - Research (网络搜索)
   - Reflection (反思分析)
   - Finalizing (生成答案)
5. 查看带引用的最终答案

### CLI 快速测试

也可以使用命令行快速测试:

```bash
cd backend
python examples/cli_research.py "What are the latest trends in renewable energy?"
```

---

## 🧠 LangGraph 基本概念

在深入学习之前,先理解几个核心概念:

### 1. 状态图 (State Graph)

LangGraph 使用**有向图**来编排 AI Agent 的工作流程。图由以下元素组成:

- **节点 (Nodes)**: 执行具体任务的函数
- **边 (Edges)**: 连接节点的路径
- **状态 (State)**: 在节点间流动的数据

### 2. 节点类型

- **普通节点**: 执行单一任务
- **条件路由节点**: 根据状态决定下一步
- **并行节点**: 同时执行多个任务

### 3. Gemini 模型选择

本项目使用两种 Gemini 模型:

- **Gemini 2.0 Flash** (`gemini-2.0-flash-exp`)
  - 速度快
  - 用于: 查询生成、快速反思

- **Gemini 2.5 Pro** (`gemini-2.5-pro-exp-03-25`)
  - 推理能力强
  - 用于: 深度反思、最终答案生成

---

## 🎨 UI 功能特性

### 研究深度 (Effort) 配置

用户可以选择三种研究深度:

| Effort  | 查询数量 | 最大循环次数 | 适用场景 |
|---------|---------|-------------|---------|
| Low     | 1       | 1           | 简单问题 |
| Medium  | 3       | 3           | 中等复杂度 |
| High    | 5       | 10          | 复杂研究 |

### 实时活动时间线

前端实时显示 Agent 的活动:

- **Generating Queries** - 生成搜索查询
- **Research** - 执行网络搜索
- **Reflection** - 分析知识缺口
- **Finalizing** - 生成最终答案

### 消息渲染

- **Human 消息**: 用户输入,显示在右侧
- **AI 消息**: Agent 回答,显示在左侧,带 Markdown 渲染
- **引用链接**: 以 Badge 形式显示,点击可跳转

---

## 🔍 开发环境 vs 生产环境

### 开发环境

- **前端**: Vite Dev Server (端口 5173)
- **后端**: LangGraph Dev Server (端口 2024)
- **热重载**: 前后端都支持
- **数据库**: 内存存储 (无需 Redis/PostgreSQL)

### 生产环境

- **统一端口**: 8123
- **前端**: 构建后的静态文件,由后端服务
- **数据库**: Redis (消息代理) + PostgreSQL (状态持久化)
- **部署**: Docker 容器化

---

## 📊 Agent 工作流程概览

```
用户输入
  ↓
生成初始查询 (Gemini Flash)
  ↓
并行网络搜索 (Google Search API)
  ↓
反思与知识缺口分析 (Gemini Flash)
  ↓
是否充分? ← ┐
  │ 否      │
  └─────────┘ (迭代最多 10 次)
  ↓ 是
生成最终答案 (Gemini Pro)
  ↓
返回带引用的答案
```

---

## ✅ 阶段 0 总结

### 关键收获

1. **架构理解**: 这是一个前后端分离的全栈 AI 应用
2. **技术栈**: React + Vite (前端) + LangGraph + FastAPI (后端)
3. **核心价值**: 展示了如何使用 LangGraph 构建复杂的 AI Agent
4. **环境要求**: Python 3.11+, Node.js 18+, Gemini API Key

### 下一步

进入**阶段 1: 后端核心 - 状态管理**,深入学习:
- `state.py` - 状态定义和层次结构
- `configuration.py` - 配置管理
- `tools_and_schemas.py` - 数据模式

### 学习验证

在进入下一阶段前,确保能够:

- [ ] 成功运行项目
- [ ] 在 LangGraph UI 中查看图结构
- [ ] 完成一次完整的查询测试
- [ ] 理解 Agent 的基本工作流程

---

## 📝 常见问题

### Q1: langgraph dev 命令找不到?

**A**: 确保已经激活虚拟环境并安装了依赖:

```bash
cd backend
. .venv/bin/activate  # Linux/Mac
.venv\Scripts\activate  # Windows
pip install .
```

### Q2: 前端无法连接后端?

**A**: 检查 `frontend/src/App.tsx` 中的 `apiUrl` 配置:
- 开发环境: `http://localhost:2024`
- 生产环境: `http://localhost:8123`

### Q3: Gemini API 报错?

**A**: 确认:
- API Key 正确配置在 `backend/.env`
- API Key 有效且有配额
- 网络可以访问 Google API

---

## 📚 延伸阅读

- [LangGraph 官方文档](https://langchain-ai.github.io/langgraph/)
- [Google Gemini API 文档](https://ai.google.dev/gemini-api/docs)
- [React 快速入门](https://react.dev/learn)
- [Vite 指南](https://vitejs.dev/guide/)

---

*下一阶段: 深入学习后端状态管理*
