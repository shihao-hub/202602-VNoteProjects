传统 Agent 框架

```python
# 通常使用向量数据库实现长期记忆
from vector_db import VectorDatabase

memory = VectorDatabase()
memory.store("user_preference", "喜欢使用 Vim")
memory.store("project_context", "正在开发电商网站")

# 检索时使用相似度搜索
results = memory.search("用户偏好")
```

> 请关注这个**相似度搜索**功能，这比模糊查询厉害太多了，这是**语义相似度**